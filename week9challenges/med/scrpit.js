

function pizzaParty (y) {
    return 8 / y ;
}

const slicedPizza = pizzaParty(2);

const slicedPizza2 = pizzaParty(3)

console.log(`Each person gets ${slicedPizza} slices of pizza.`);

console.log(`Each person gets ${slicedPizza2} slices of pizza.`);
